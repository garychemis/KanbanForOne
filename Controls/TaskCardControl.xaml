<UserControl x:Class="KanbanForOne.Controls.TaskCardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:KanbanForOne.Controls"
             AllowDrop="True"
             PreviewMouseLeftButtonDown="OnPreviewMouseLeftButtonDown"
             MouseMove="OnMouseMove"
             MouseLeftButtonUp="OnMouseLeftButtonUp"
             MouseEnter="OnMouseEnter"
             MouseLeave="OnMouseLeave"
             DragEnter="OnFileDragEnter"
             DragOver="OnFileDragOver"
             DragLeave="OnFileDragLeave"
             Drop="OnFileDrop">
    <Grid Margin="0,0,0,14">
        <Grid.Style>
            <Style TargetType="{x:Type Grid}">
                <Setter Property="Opacity" Value="1" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsExpanded}" Value="True">
                        <DataTrigger.EnterActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                     To="0.08"
                                                     Duration="0:0:0.08" />
                                </Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.EnterActions>
                        <DataTrigger.ExitActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                     To="1"
                                                     Duration="0:0:0.12" />
                                </Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.ExitActions>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Style>

        <Border x:Name="CardBorder"
                Style="{StaticResource CardBorderStyle}"
                Background="{Binding Status, Converter={StaticResource TaskStatusBackgroundConverter}}"
                BorderBrush="#EEF0F2"
                CornerRadius="8"
                MinHeight="92"
                RenderTransformOrigin="0.5,0.5">
            <Border.RenderTransform>
                <TransformGroup>
                    <ScaleTransform x:Name="DragScaleTransform"
                                    ScaleX="1"
                                    ScaleY="1" />
                    <RotateTransform x:Name="DragRotateTransform"
                                     Angle="0" />
                    <TranslateTransform x:Name="LiftTransform" />
                </TransformGroup>
            </Border.RenderTransform>

            <Grid x:Name="CardContent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Background="{Binding Status, Converter={StaticResource TaskStatusBrushConverter}}"
                        CornerRadius="8,0,0,8" />

                <Grid Grid.Column="1"
                      Margin="18,16,18,14">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="{Binding Title}"
                               FontSize="15"
                               FontWeight="SemiBold"
                               Foreground="{Binding Status, Converter={StaticResource DoneForegroundConverter}}"
                               TextDecorations="{Binding Status, Converter={StaticResource DoneTextDecorationConverter}}"
                               TextWrapping="Wrap" />

                    <controls:MarkdownPreviewControl Markdown="{Binding Description}"
                                                     Grid.Row="1"
                                                     Margin="0,6,0,0"
                                                     PreviewFontSize="12"
                                                     PreviewForeground="{StaticResource TextSecondaryBrush}"
                                                     PreviewLineHeight="17"
                                                     PreviewMaxHeight="36" />

                    <Grid Grid.Row="3"
                          Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock FontSize="11"
                                   TextTrimming="CharacterEllipsis"
                                   Foreground="{StaticResource TextSecondaryBrush}">
                            <Run Text="日期 " />
                            <Run Text="{Binding DateRangeDisplay, Mode=OneWay}" />
                        </TextBlock>

                        <TextBlock Grid.Column="1"
                                   Text="{Binding Priority, Converter={StaticResource TaskPriorityNameConverter}, StringFormat=优先级 {0}}"
                                   Margin="10,0,0,0"
                                   FontSize="11"
                                   Foreground="{Binding Priority, Converter={StaticResource TaskPriorityBrushConverter}}" />
                    </Grid>

                    <Grid Grid.Row="4"
                          Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <ItemsControl ItemsSource="{Binding Tags}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="7,3"
                                            Margin="0,0,6,4"
                                            CornerRadius="8"
                                            Background="#F3F5F7">
                                        <TextBlock Text="{Binding Converter={StaticResource TagDisplayConverter}}"
                                                   FontSize="10"
                                                   FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextSecondaryBrush}" />
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Grid.Column="1"
                                   Text="{Binding AttachmentCount, StringFormat=附件 {0}}"
                                   Margin="10,2,0,0"
                                   FontSize="11"
                                   Foreground="{StaticResource TextSecondaryBrush}" />
                    </Grid>
                </Grid>
            </Grid>
        </Border>

        <Border x:Name="DropHint"
                Visibility="Collapsed"
                CornerRadius="8"
                Background="#CCEFF6FF"
                BorderBrush="#3B82F6"
                BorderThickness="1"
                IsHitTestVisible="False">
            <TextBlock Text="释放文件以保存为附件"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="12"
                       FontWeight="SemiBold"
                       Foreground="#1D4ED8" />
        </Border>
    </Grid>
</UserControl>
